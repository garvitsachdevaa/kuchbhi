from setuptools import setup, find_packages

setup(
    name="spindleflow-rl",
    version="0.1.0",
    packages=find_packages(),
    python_requires=">=3.10",
    description="RL environment for SpindleFlow orchestration delegation policy",
)
